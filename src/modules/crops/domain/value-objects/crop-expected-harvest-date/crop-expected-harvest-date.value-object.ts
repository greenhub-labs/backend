import { DateValueObject } from 'src/shared/domain/value-objects/date.value-object';

/**
 * Value Object representing the expected harvest date of a crop.
 * Extends the generic DateValueObject for domain-specific semantics.
 *
 * @extends DateValueObject
 */
export class CropExpectedHarvestDateValueObject extends DateValueObject {}
