import { Module } from '@nestjs/common';

/**
 * CropVarietyDomainModule
 * Domain layer for CropVariety (DDD Clean Architecture)
 */
@Module({
  providers: [
    // Entities, ValueObjects, DomainServices, Factories, etc.
  ],
  exports: [
    // Entities, ValueObjects, DomainServices, Factories, etc.
  ],
})
export class CropVarietyDomainModule {}
