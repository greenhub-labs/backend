/**
 * Command to delete an existing farms.
 * Add more properties as needed for your use case.
 */
export class DeleteFarmsCommand {
  /** Example property (replace or extend as needed) */
  // public readonly id: string;

  constructor(
    // id: string,
    // Add more parameters as needed
  ) {
    // this.id = id;
    // Assign more properties here if needed
  }
} 