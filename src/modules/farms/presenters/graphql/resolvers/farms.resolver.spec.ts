import { FarmsResolver } from './farms.resolver';

describe('FarmsResolver', () => {
  it('should be defined', () => {
    const resolver = new FarmsResolver();
    expect(resolver).toBeDefined();
  });
}); 