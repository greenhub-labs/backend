/**
 * ===================================================================
 * 🌿 GREENHUB LABS - PRISMA SCHEMA
 * ===================================================================
 * 
 * Sistema de automatización agrícola inteligente con arquitectura multitenant.
 * Gestiona huertos, gallineros, sistemas IoT, riego automático, energía solar
 * y análisis predictivo mediante IA.
 * 
 * @version 1.0.0
 * @author GreenHub Labs Team
 * @description Base de datos para plataforma de agricultura inteligente
 * 
 * ARQUITECTURA:
 * - Multitenant: Múltiples farms por usuario
 * - IoT Ready: Nodos distribuidos con sensores
 * - AI Integration: Predicciones y automatización
 * - Energy Management: Solar + Battery monitoring
 * 
 * ===================================================================
 */

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "linux-arm64-openssl-3.0.x", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ================================
// 🏷️ ENUMS & TYPE DEFINITIONS
// ================================

/// Roles de usuario dentro de una farm
/// Define los niveles de acceso y permisos
enum Roles {
  SYSTEM        /// Administrador del sistema: acceso total, puede gestionar todos los recursos.
  OWNER         /// Propietario: acceso total, puede eliminar farm, usuarios, etc.
  ADMINISTRATOR /// Administrador: gestión completa excepto eliminación
  MANAGER       /// Gestor: control operativo de cultivos y sistemas, puede eliminar farm, usuarios, etc.
  VIEWER        /// Observador: solo lectura de datos y reportes
  GUEST         /// Invitado: acceso limitado a información básica
}

/// Estado actual de un bancal/plot
enum PlotStatus {
  ACTIVE    /// En uso activo con cultivos
  INACTIVE  /// Temporalmente fuera de uso
  PREPARING /// En preparación para nueva siembra
  RESTING   /// En barbecho para recuperación del suelo
}

/// Estado del ciclo de vida de un cultivo
enum CropStatus {
  PLANNED    /// Planificado pero no plantado
  PLANTED    /// Recién plantado o sembrado
  GROWING    /// En crecimiento vegetativo
  FLOWERING  /// En floración
  FRUITING   /// Produciendo frutos
  HARVESTING /// En proceso de cosecha
  FINISHED   /// Ciclo completado
}

/// Categoría del tipo de cultivo
enum CropType {
  VEGETABLE /// Hortalizas (tomate, lechuga, etc.)
  FRUIT     /// Frutales (manzanas, fresas, etc.)
  HERB      /// Hierbas aromáticas y medicinales
  GRAIN     /// Cereales (trigo, avena, etc.)
  LEGUME    /// Leguminosas (judías, garbanzos, etc.)
  ROOT      /// Tubérculos (patatas, zanahorias, etc.)
  FLOWER    /// Plantas ornamentales
}

/// Estaciones del año para planificación
enum Season {
  SPRING /// Primavera: marzo-mayo
  SUMMER /// Verano: junio-agosto
  AUTUMN /// Otoño: septiembre-noviembre
  WINTER /// Invierno: diciembre-febrero
}

/// Método de plantación utilizado
enum PlantingMethod {
  DIRECT_SEED /// Siembra directa en el suelo
  TRANSPLANT  /// Trasplante desde semillero
  CUTTING     /// Propagación por esqueje
  BULB        /// Plantación de bulbos
}

/// Tipos de sensores IoT disponibles
enum SensorType {
  SOIL_MOISTURE    /// Humedad del suelo (%)
  TEMPERATURE      /// Temperatura ambiente (°C)
  HUMIDITY         /// Humedad relativa del aire (%)
  PH              /// Acidez del suelo (0-14)
  LIGHT           /// Luminosidad ambiental (lux)
  PRESSURE        /// Presión atmosférica (hPa)
  CO2             /// Concentración de CO2 (ppm)
  NH3             /// Concentración de amoníaco (ppm)
  WATER_FLOW      /// Flujo de agua (l/min)
  WATER_PRESSURE  /// Presión del agua (bar)
  BATTERY_LEVEL   /// Nivel de batería (%)
  SOLAR_GENERATION /// Generación solar (W)
}

/// Estado del sistema de riego
enum IrrigationStatus {
  INACTIVE  /// Sistema apagado
  ACTIVE    /// Regando actualmente
  SCHEDULED /// Programado para regar
  MANUAL    /// Control manual activo
  ERROR     /// Error en el sistema
}

/// Estado de las válvulas de riego
enum ValveStatus {
  OPEN    /// Válvula abierta (agua fluyendo)
  CLOSED  /// Válvula cerrada
  PARTIAL /// Parcialmente abierta
  ERROR   /// Error en la válvula
}

/// Nivel de severidad de alertas
enum AlertLevel {
  INFO     /// Información general
  WARNING  /// Advertencia que requiere atención
  CRITICAL /// Crítico, requiere acción inmediata
  ERROR    /// Error del sistema
}

/// Unidades de medida específicas para dimensiones
enum UnitMeasurement {
  // Unidades métricas
  METERS       /// Metros (m)
  CENTIMETERS  /// Centímetros (cm)
  KILOMETERS   /// Kilómetros (km)
  MILLIMETERS  /// Milímetros (mm)
  
  // Unidades imperiales
  FEET         /// Pies (ft)
  INCHES       /// Pulgadas (in)
  YARDS        /// Yardas (yd)
  MILES        /// Millas (mi)
}

/// Tipos de alertas del sistema
enum AlertType {
  SENSOR_FAILURE        /// Fallo de sensor IoT
  LOW_BATTERY          /// Batería baja en nodo
  IRRIGATION_ISSUE     /// Problema en sistema de riego
  WEATHER_ALERT        /// Alerta meteorológica
  SECURITY_BREACH      /// Brecha de seguridad
  SYSTEM_ERROR         /// Error general del sistema
  MAINTENANCE_REQUIRED /// Mantenimiento requerido
}

/// Canales de notificación disponibles
enum NotificationChannel {
  EMAIL    /// Correo electrónico
  SMS      /// Mensaje de texto
  PUSH     /// Notificación push móvil
  TELEGRAM /// Bot de Telegram
  SLACK    /// Canal de Slack
  DISCORD  /// Canal de Discord
}

/// Razas de gallinas comunes
enum ChickenBreed {
  LEGHORN          /// Leghorn (alta producción de huevos)
  RHODE_ISLAND_RED /// Rhode Island Red (doble propósito)
  PLYMOUTH_ROCK    /// Plymouth Rock (raza dócil)
  SUSSEX           /// Sussex (buena ponedora)
  ORPINGTON        /// Orpington (raza ornamental)
  AUSTRALORP       /// Australorp (excelente ponedora)
  MARANS           /// Marans (huevos oscuros)
  BANTAM           /// Bantam (raza miniatura)
  OTHER            /// Otra raza no especificada
}

/// Estado de salud de las gallinas
enum ChickenStatus {
  HEALTHY    /// Saludable y activa
  SICK       /// Enferma, requiere atención
  QUARANTINE /// En cuarentena por enfermedad
  BROODING   /// Empollando huevos
  MOLTING    /// En proceso de muda
  RETIRED    /// Retirada de producción
  DECEASED   /// Fallecida
}

/// Calidad de los huevos recolectados
enum EggQuality {
  EXCELLENT /// Excelente calidad para venta
  GOOD      /// Buena calidad para consumo
  FAIR      /// Calidad aceptable
  POOR      /// Baja calidad
  CRACKED   /// Huevo agrietado
  DIRTY     /// Huevo sucio
}

/// Estado de las puertas automáticas del gallinero
enum DoorStatus {
  OPEN    /// Puerta abierta
  CLOSED  /// Puerta cerrada
  OPENING /// Abriéndose
  CLOSING /// Cerrándose
  ERROR   /// Error en mecanismo
}

/// Modo de operación de la automatización
enum AutomationMode {
  MANUAL        /// Control manual exclusivo
  SCHEDULED     /// Basado en horarios programados
  SENSOR_BASED  /// Basado en lecturas de sensores
  AI_CONTROLLED /// Controlado por IA predictiva
}

/// Condiciones meteorológicas
enum WeatherCondition {
  SUNNY  /// Soleado
  CLOUDY /// Nublado
  RAINY  /// Lluvioso
  STORMY /// Tormenta
  SNOWY  /// Nevando
  FOGGY  /// Niebla
  WINDY  /// Ventoso
}

/// Fuente de energía del sistema
enum EnergySource {
  SOLAR   /// Energía solar exclusiva
  BATTERY /// Batería solamente
  GRID    /// Red eléctrica
  HYBRID  /// Sistema híbrido (solar + red)
}

/// Estado del sistema IoT
enum SystemStatus {
  ONLINE      /// En línea y operativo
  OFFLINE     /// Fuera de línea
  MAINTENANCE /// En mantenimiento
  ERROR       /// Error en el sistema
}

enum SoilType {
  SANDY      /// Sandy soil - Loose texture, drains quickly, low fertility
  CLAY       /// Clay soil - Heavy, retains water well, fertile but hard to work
  SILTY      /// Silty soil - Smooth texture, good water retention, easily compacted
  LOAM       /// Loam soil - Balanced mix of sand, silt and clay, ideal for most crops
  CALCAREOUS /// Calcareous soil - Rich in limestone, alkaline, can limit nutrient uptake
  PEAT       /// Peat soil - High in organic matter, very fertile, wet and acidic
  SALINE     /// Saline soil - Contains excess soluble salts, can affect plant growth
  VOLCANIC   /// Volcanic soil (Andosol) - Very fertile, high mineral content, excellent for high-value crops
}

// ================================
// 👥 USER MANAGEMENT & MULTI-TENANT
// ================================

/**
 * Usuario del sistema
 * 
 * Representa a una persona que puede acceder al sistema y pertenecer
 * a múltiples farms con diferentes roles. El sistema es multitenant,
 * permitiendo que un usuario gestione varios huertos/granjas.
 */
model User {
  /// UUID único del usuario
  id        String   @id @default(uuid())
  /// Nombre de pila
  firstName String?
  /// Apellidos
  lastName  String?
  /// Biografía o descripción personal
  bio       String?
  /// URL del avatar/foto de perfil
  avatar    String?
  /// Si el usuario está activo en el sistema
  isActive  Boolean  @default(true)
  /// Soft delete: usuario eliminado lógicamente
  isDeleted Boolean  @default(false)
  /// Fecha de creación del registro
  createdAt DateTime @default(now())
  /// Fecha de última actualización
  updatedAt DateTime @updatedAt
  /// Fecha de eliminación del registro
  deletedAt DateTime?
  
  // Relations
  /// Datos de autenticación del usuario
  auth            Auth?
  /// Membresías a farms (un usuario puede estar en múltiples farms)
  farmMemberships FarmMembership[]
  /// Logs de riego realizados por el usuario
  irrigationLogs  IrrigationLog[]
  /// Registros de cosechas realizadas
  harvestLogs     HarvestLog[]
  /// Registros de siembras realizadas
  plantingLogs    PlantingLog[]
  /// Logs de mantenimiento realizados
  maintenanceLogs MaintenanceLog[]
  /// Notificaciones recibidas
  notifications   Notification[]
  /// Interacciones con el asistente IA
  aiInteractions  AIInteraction[]
  /// Alertas creadas por el usuario
  createdAlerts   Alert[]       @relation("AlertCreatedBy")
  /// Alertas asignadas al usuario
  assignedAlerts  Alert[]       @relation("AlertAssignedTo")
  
  @@map("users")
}

/**
 * Datos de autenticación del usuario
 * 
 * Almacena credenciales y información de sesión.
 * Separado del modelo User para mayor seguridad.
 */
model Auth {
  /// UUID único del registro de autenticación
  id         String    @id @default(uuid())
  /// Referencia al usuario (relación 1:1)
  userId     String    @unique
  /// Email único para login
  email      String    @unique
  /// Teléfono opcional para 2FA
  phone      String?   @unique
  /// Hash de la contraseña (bcrypt)
  password   String?
  /// Si el email ha sido verificado
  isVerified Boolean   @default(false)
  /// Último inicio de sesión
  lastLogin  DateTime?
  /// Fecha de creación
  createdAt  DateTime  @default(now())
  /// Fecha de última actualización
  updatedAt  DateTime  @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt  DateTime?
  
  // Relations
  /// Usuario asociado
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@map("auth")
}

/**
 * Farm/Granja - Entidad principal multitenant
 * 
 * Representa un huerto, granja o explotación agrícola.
 * Todos los recursos (plots, sensores, etc.) pertenecen a una farm.
 * Un usuario puede pertenecer a múltiples farms.
 */
model Farm {
  /// UUID único de la farm
  id          String   @id @default(uuid())
  /// Nombre de la farm
  name        String
  /// Descripción opcional
  description String?
  /// Pais de la farm
  country     String?
  /// Estado o provincia de la farm
  state       String?
  /// Ciudad de la farm
  city        String?
  /// Código postal de la farm
  postalCode  String?
  /// Dirección completa estructurada
  street      String?
  /// Coordenadas GPS
  latitude    Float?
  longitude   Float?
  /// Si la farm está activa
  isActive    Boolean  @default(true)
  /// Fecha de creación
  createdAt   DateTime @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Usuarios miembros de la farm
  memberships     FarmMembership[]
  /// Bancales/parcelas de cultivo
  plots           Plot[]
  /// Gallineros de la farm
  coops           Coop[]
  /// Nodos IoT instalados
  iotNodes        IoTNode[]
  /// Zonas de riego configuradas
  irrigationZones IrrigationZone[]
  /// Datos meteorológicos locales
  weatherData     WeatherData[]
  /// Paneles solares instalados
  solarPanels     SolarPanel[]
  /// Baterías del sistema
  batteries       Battery[]
  /// Alertas generadas en la farm
  alerts          Alert[]
  /// Configuraciones del sistema
  configurations  FarmConfiguration[]
  
  @@map("farms")
}

/**
 * Roles del sistema
 * 
 * Define los diferentes niveles de acceso y permisos
 * que puede tener un usuario dentro de una farm.
 */
model Role {
  /// UUID único del rol
  id          String   @id @default(uuid())
  /// Nombre del rol (del enum Roles)
  name        Roles    @unique
  /// Descripción del rol y sus permisos
  description String?
  /// Fecha de creación
  createdAt   DateTime @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Membresías que usan este rol
  farmMemberships FarmMembership[]
  
  @@map("roles")
}

/**
 * Membresía de usuario a farm
 * 
 * Tabla de relación many-to-many entre User y Farm,
 * con información adicional como el rol del usuario en esa farm.
 * Un usuario puede ser OWNER de una farm y VIEWER de otra.
 */
model FarmMembership {
  /// UUID único de la membresía
  id        String   @id @default(uuid())
  /// Referencia al usuario
  userId    String
  /// Referencia a la farm
  farmId    String
  /// Referencia al rol del usuario en esta farm
  roleId    String
  /// Si la membresía está activa
  isActive  Boolean  @default(true)
  /// Fecha en que se unió a la farm
  joinedAt  DateTime @default(now())
  /// Fecha de última actualización
  updatedAt DateTime @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  // Relations
  /// Usuario miembro
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  /// Farm a la que pertenece
  farm Farm @relation(fields: [farmId], references: [id], onDelete: Cascade)
  /// Rol en esta farm
  role Role @relation(fields: [roleId], references: [id])
  
  /// Un usuario solo puede tener una membresía por farm
  @@unique([userId, farmId])
  @@map("farm_memberships")
}

// ================================
// 🌱 GARDEN MANAGEMENT
// ================================

/**
 * Bancal/Parcela de cultivo
 * 
 * Representa una zona física delimitada donde se cultivan plantas.
 * Cada plot pertenece a una farm y puede tener múltiples cultivos
 * a lo largo del tiempo (rotación de cultivos).
 */
model Plot {
  /// UUID único del bancal
  id          String     @id @default(uuid())
  /// Nombre descriptivo del bancal
  name        String
  /// Descripción adicional del bancal
  description String?
  /// Ancho en metros
  width       Float?
  /// Longitud en metros
  length      Float?
  /// Altura en metros
  height      Float?
  /// Área en metros cuadrados
  area        Float? 
  /// Perímetro en metros
  perimeter   Float?
  /// Volumen en metros cúbicos
  volume      Float?
  /// Unidad de medida específica para las dimensiones
  unitMeasurement UnitMeasurement? @default(METERS)
  /// Estado actual del bancal
  status      PlotStatus @default(ACTIVE)
  /// Tipo de suelo (arcilloso, arenoso, etc.)
  soilType    SoilType? @default(LOAM)
  /// Nivel de pH del suelo (0-14)
  soilPh      Float?
  /// Farm a la que pertenece este bancal
  farmId      String
  /// Fecha de creación
  createdAt   DateTime   @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime   @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Farm propietaria del bancal
  farm            Farm             @relation(fields: [farmId], references: [id])
  /// Cultivos plantados en este bancal
  crops           Crop[]
  /// Nodos IoT instalados en el bancal
  iotNodes        IoTNode[]
  /// Zonas de riego del bancal
  irrigationZones IrrigationZone[]
  /// Lecturas de sensores en el bancal
  sensorReadings  SensorReading[]
  
  @@map("plots")
}

/**
 * Variedad de cultivo
 * 
 * Catálogo de plantas y variedades disponibles para cultivar.
 * Contiene información técnica sobre requerimientos de crecimiento,
 * compatibilidades y características de cada variedad.
 */
model CropVariety {
  /// UUID único de la variedad
  id                String        @id @default(uuid())
  /// Nombre común de la variedad
  name              String
  /// Nombre científico (ej: Solanum lycopersicum)
  scientificName    String?
  /// Categoría del cultivo
  type              CropType
  /// Descripción detallada de la variedad
  description       String?
  /// Rendimiento promedio en kg por m²
  averageYield      Float?
  /// Días desde siembra hasta cosecha
  daysToMaturity    Int?
  /// Profundidad de siembra en centímetros
  plantingDepth     Float?
  /// Espaciado entre plantas en centímetros
  spacingBetween    Float?
  /// Requerimientos hídricos
  waterRequirements String? // "low", "medium", "high"
  /// Requerimientos de luz solar
  sunRequirements   String? // "full", "partial", "shade"
  /// Temperatura ideal de crecimiento
  idealTemperature  Json? // { min: number, max: number }
  /// Rango de pH ideal del suelo
  idealPh           Json? // { min: number, max: number }
  /// Plantas compatibles para siembra asociada
  compatibleWith    String[]
  /// Plantas incompatibles (evitar cercanía)
  incompatibleWith  String[]
  /// Estaciones ideales para plantación
  plantingSeasons   Season[]
  /// Estaciones de cosecha
  harvestSeasons    Season[]
  /// Fecha de creación del registro
  createdAt         DateTime      @default(now())
  /// Fecha de última actualización
  updatedAt         DateTime      @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt         DateTime?
  
  // Relations
  /// Cultivos que usan esta variedad
  crops Crop[]
  
  @@map("crop_varieties")
}

/**
 * Cultivo activo
 * 
 * Representa una instancia específica de un cultivo plantado
 * en un bancal determinado. Incluye fechas, estado actual
 * y seguimiento del ciclo de vida de la plantación.
 */
model Crop {
  /// UUID único del cultivo
  id             String         @id @default(uuid())
  /// Bancal donde está plantado
  plotId         String
  /// Variedad de cultivo utilizada
  varietyId      String
  /// Fecha real de plantación/siembra
  plantingDate   DateTime?
  /// Fecha estimada de cosecha
  expectedHarvest DateTime?
  /// Fecha real de cosecha completada
  actualHarvest  DateTime?
  /// Número de plantas o unidades sembradas
  quantity       Int?
  /// Estado actual del cultivo
  status         CropStatus     @default(PLANNED)
  /// Método de plantación utilizado
  plantingMethod PlantingMethod
  /// Notas adicionales del agricultor
  notes          String?
  /// Fecha de creación del registro
  createdAt      DateTime       @default(now())
  /// Fecha de última actualización
  updatedAt      DateTime       @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt      DateTime?
  
  // Relations
  /// Bancal donde está plantado
  plot         Plot          @relation(fields: [plotId], references: [id])
  /// Variedad de cultivo
  variety      CropVariety   @relation(fields: [varietyId], references: [id])
  /// Registros de cosechas de este cultivo
  harvestLogs  HarvestLog[]
  /// Registros de plantación
  plantingLogs PlantingLog[]
  
  @@map("crops")
}

/**
 * Registro de cosecha
 * 
 * Documenta cada sesión de cosecha realizada en un cultivo,
 * incluyendo cantidad, calidad y observaciones.
 */
model HarvestLog {
  /// UUID único del registro de cosecha
  id          String   @id @default(uuid())
  /// Cultivo del que se cosecha
  cropId      String
  /// Usuario que realizó la cosecha
  userId      String
  /// Cantidad cosechada (kg o unidades)
  quantity    Float
  /// Calidad de la cosecha
  quality     String? // "excellent", "good", "fair", "poor"
  /// Observaciones sobre la cosecha
  notes       String?
  /// Fecha en que se realizó la cosecha
  harvestDate DateTime @default(now())
  /// Fecha de creación del registro
  createdAt   DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Cultivo cosechado
  crop Crop @relation(fields: [cropId], references: [id])
  /// Usuario que realizó la cosecha
  user User @relation(fields: [userId], references: [id])
  
  @@map("harvest_logs")
}

/**
 * Registro de plantación
 * 
 * Documenta las actividades de siembra y plantación
 * realizadas en cada cultivo.
 */
model PlantingLog {
  /// UUID único del registro de plantación
  id           String   @id @default(uuid())
  /// Cultivo al que pertenece la plantación
  cropId       String
  /// Usuario que realizó la plantación
  userId       String
  /// Cantidad de plantas/semillas
  quantity     Int
  /// Método de plantación empleado
  method       PlantingMethod
  /// Observaciones de la plantación
  notes        String?
  /// Fecha en que se realizó la plantación
  plantingDate DateTime @default(now())
  /// Fecha de creación del registro
  createdAt    DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt    DateTime?
  
  // Relations
  /// Cultivo plantado
  crop Crop @relation(fields: [cropId], references: [id])
  /// Usuario que realizó la plantación
  user User @relation(fields: [userId], references: [id])
  
  @@map("planting_logs")
}

// ================================
// 🐔 CHICKEN COOP MANAGEMENT
// ================================
// Gestión completa de gallineros: aves, producción de huevos,
// control ambiental y automatización de puertas

/**
 * Gallina individual
 * 
 * Registro de cada ave en el gallinero con seguimiento
 * de salud, producción y características físicas.
 */
model Chicken {
  /// UUID único de la gallina
  id          String        @id @default(uuid())
  /// Nombre opcional de la gallina
  name        String?
  /// Raza de la gallina
  breed       ChickenBreed
  /// Fecha de nacimiento o eclosión
  hatchDate   DateTime?
  /// Fecha en que se adquirió la gallina
  acquiredDate DateTime     @default(now())
  /// Estado actual de salud
  status      ChickenStatus @default(HEALTHY)
  /// Color o descripción física
  color       String?
  /// Peso aproximado en kilogramos
  weight      Float?
  /// Si es gallina ponedora o no
  isLayer     Boolean       @default(true)
  /// Notas adicionales sobre la gallina
  notes       String?
  /// Gallinero donde vive (opcional)
  coopId      String?
  /// Fecha de creación del registro
  createdAt   DateTime      @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime      @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Gallinero donde vive
  coop    Coop?     @relation(fields: [coopId], references: [id])
  /// Registros de puesta de huevos
  eggLogs EggLog[]
  
  @@map("chickens")
}

/**
 * Gallinero
 * 
 * Estructura física que aloja gallinas con control
 * ambiental y automatización de puertas.
 */
model Coop {
  /// UUID único del gallinero
  id          String      @id @default(uuid())
  /// Nombre descriptivo del gallinero
  name        String
  /// Descripción adicional
  description String?
  /// Capacidad máxima de gallinas
  capacity    Int
  /// Ubicación GPS del gallinero
  location    Json? // { lat: number, lng: number }
  /// Dimensiones físicas
  dimensions  Json? // { length: number, width: number, height: number }
  /// Si tiene área de paseo exterior
  hasRun      Boolean     @default(false)
  /// Área del paseo en metros cuadrados
  runArea     Float?
  /// Farm a la que pertenece
  farmId      String
  /// Fecha de creación
  createdAt   DateTime    @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime    @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Farm propietaria
  farm           Farm            @relation(fields: [farmId], references: [id])
  /// Gallinas que viven en este gallinero
  chickens       Chicken[]
  /// Nodos IoT instalados en el gallinero
  iotNodes       IoTNode[]
  /// Puertas automatizadas del gallinero
  doors          CoopDoor[]
  /// Lecturas de sensores ambientales
  sensorReadings SensorReading[]
  
  @@map("coops")
}

/**
 * Puerta automática del gallinero
 * 
 * Control automatizado de apertura/cierre basado
 * en horarios o sensores de luz solar.
 */
model CoopDoor {
  /// UUID único de la puerta
  id            String     @id @default(uuid())
  /// Gallinero al que pertenece
  coopId        String
  /// Nombre descriptivo de la puerta
  name          String
  /// Estado actual de la puerta
  status        DoorStatus @default(CLOSED)
  /// Si está en modo automático
  isAutomatic   Boolean    @default(true)
  /// Hora fija de apertura (HH:MM)
  openTime      String?
  /// Hora fija de cierre (HH:MM)
  closeTime     String?
  /// Usar horarios de amanecer/atardecer
  useSunriseSunset Boolean @default(true)
  /// Offset en minutos del amanecer para abrir
  sunriseOffset Int       @default(0)
  /// Offset en minutos del atardecer para cerrar
  sunsetOffset  Int       @default(0)
  /// Fecha de creación
  createdAt     DateTime   @default(now())
  /// Fecha de última actualización
  updatedAt     DateTime   @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt     DateTime?
  
  // Relations
  /// Gallinero al que pertenece
  coop     Coop       @relation(fields: [coopId], references: [id])
  /// Logs de actividad de la puerta
  doorLogs DoorLog[]
  
  @@map("coop_doors")
}

/**
 * Registro de actividad de puerta
 * 
 * Log de cada apertura/cierre de puerta con
 * información sobre el trigger que lo causó.
 */
model DoorLog {
  /// UUID único del log
  id        String     @id @default(uuid())
  /// Puerta que generó el evento
  doorId    String
  /// Estado resultante de la puerta
  status    DoorStatus
  /// Qué causó el cambio de estado
  trigger   String // "manual", "scheduled", "sensor", "emergency"
  /// Momento en que ocurrió el evento
  timestamp DateTime   @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  // Relations
  /// Puerta que generó el log
  door CoopDoor @relation(fields: [doorId], references: [id])
  
  @@map("door_logs")
}

/**
 * Registro de recolección de huevos
 * 
 * Documenta cada sesión de recolección con
 * información de calidad y cantidad.
 */
model EggLog {
  /// UUID único del registro
  id        String     @id @default(uuid())
  /// Gallina que puso el huevo (si se conoce)
  chickenId String?
  /// Gallinero donde se encontró (si gallina desconocida)
  coopId    String?
  /// Cantidad de huevos recolectados
  quantity  Int        @default(1)
  /// Peso total en gramos
  weight    Float?
  /// Calidad de los huevos
  quality   EggQuality @default(GOOD)
  /// Observaciones adicionales
  notes     String?
  /// Fecha y hora de recolección
  collectedAt DateTime @default(now())
  /// Fecha de creación del registro
  createdAt DateTime   @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  // Relations
  /// Gallina que puso el huevo
  chicken Chicken? @relation(fields: [chickenId], references: [id])
  
  @@map("egg_logs")
}

// ================================
// 📡 IOT AND SENSOR MANAGEMENT
// ================================
// Red distribuida de nodos IoT con sensores múltiples,
// comunicación mesh y gestión energética solar

/**
 * Nodo IoT
 * 
 * Dispositivo físico distribuido en la farm que contiene
 * múltiples sensores y se comunica con el sistema central.
 * Cada nodo tiene gestión energética y conectividad mesh.
 */
model IoTNode {
  /// UUID único del nodo IoT
  id             String       @id @default(uuid())
  /// Nombre descriptivo del nodo
  name           String
  /// Descripción adicional del nodo
  description    String?
  /// Dirección MAC única del dispositivo
  macAddress     String       @unique
  /// Dirección IP actual en la red
  ipAddress      String?
  /// Versión del firmware instalado
  firmwareVersion String?
  /// Último heartbeat recibido del nodo
  lastHeartbeat  DateTime?
  /// Nivel de batería en porcentaje (0-100)
  batteryLevel   Float?
  /// Intensidad de señal RSSI en dBm
  signalStrength Float?
  /// Estado actual del sistema
  status         SystemStatus @default(OFFLINE)
  /// Ubicación GPS del nodo
  location       Json? // { lat: number, lng: number, altitude?: number }
  /// Bancal donde está instalado (opcional)
  plotId         String?
  /// Gallinero donde está instalado (opcional)
  coopId         String?
  /// Farm a la que pertenece
  farmId         String
  /// Fuente de energía principal
  energySource   EnergySource @default(BATTERY)
  /// Fecha de instalación
  createdAt      DateTime     @default(now())
  /// Fecha de última actualización
  updatedAt      DateTime     @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt      DateTime?
  
  // Relations
  /// Farm propietaria del nodo
  farm           Farm            @relation(fields: [farmId], references: [id])
  /// Bancal donde está instalado
  plot           Plot?           @relation(fields: [plotId], references: [id])
  /// Gallinero donde está instalado
  coop           Coop?           @relation(fields: [coopId], references: [id])
  /// Sensores conectados al nodo
  sensors        Sensor[]
  /// Lecturas de sensores del nodo
  sensorReadings SensorReading[]
  /// Alertas generadas por el nodo
  alerts         Alert[]
  
  @@map("iot_nodes")
}

/**
 * Sensor físico
 * 
 * Dispositivo de medición específico conectado a un nodo IoT.
 * Cada sensor mide un parámetro ambiental específico con
 * sus propios rangos y calibración.
 */
model Sensor {
  /// UUID único del sensor
  id          String     @id @default(uuid())
  /// Nodo IoT al que está conectado
  nodeId      String
  /// Tipo de sensor y qué mide
  type        SensorType
  /// Modelo específico del sensor
  model       String?
  /// Parámetros de calibración del sensor
  calibration Json? // { offset: number, multiplier: number, ... }
  /// Valor mínimo que puede medir
  minValue    Float?
  /// Valor máximo que puede medir
  maxValue    Float?
  /// Unidad de medida del sensor
  unit        String
  /// Si el sensor está actualmente activo
  isActive    Boolean    @default(true)
  /// Fecha de instalación
  createdAt   DateTime   @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime   @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Nodo IoT al que pertenece
  node     IoTNode         @relation(fields: [nodeId], references: [id])
  /// Lecturas tomadas por este sensor
  readings SensorReading[]
  
  @@map("sensors")
}

/**
 * Lectura de sensor
 * 
 * Medición individual tomada por un sensor en un momento específico.
 * Incluye información de calidad del dato y contexto de ubicación.
 */
model SensorReading {
  /// UUID único de la lectura
  id        String   @id @default(uuid())
  /// Nodo que tomó la medición
  nodeId    String
  /// Sensor específico que tomó la medición
  sensorId  String
  /// Bancal donde se tomó la medición (si aplica)
  plotId    String?
  /// Gallinero donde se tomó la medición (si aplica)
  coopId    String?
  /// Valor medido por el sensor
  value     Float
  /// Unidad de la medición
  unit      String
  /// Calidad del dato (0-1, donde 1 es perfecta)
  quality   Float?
  /// Momento exacto de la medición
  timestamp DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  // Relations
  /// Nodo que generó la lectura
  node   IoTNode  @relation(fields: [nodeId], references: [id])
  /// Sensor que tomó la medición
  sensor Sensor   @relation(fields: [sensorId], references: [id])
  /// Bancal donde se midió
  plot   Plot?    @relation(fields: [plotId], references: [id])
  /// Gallinero donde se midió
  coop   Coop?    @relation(fields: [coopId], references: [id])
  
  /// Índices para consultas eficientes por nodo y tiempo
  @@index([nodeId, timestamp])
  /// Índices para consultas eficientes por sensor y tiempo
  @@index([sensorId, timestamp])
  @@map("sensor_readings")
}

// ================================
// 💧 IRRIGATION SYSTEM
// ================================
// Sistema de riego inteligente: zonas independientes,
// válvulas automatizadas y programación flexible

/**
 * Zona de riego
 * 
 * Área delimitada con sistema de riego independiente.
 * Cada zona puede tener múltiples válvulas y programaciones
 * específicas según las necesidades de los cultivos.
 */
model IrrigationZone {
  /// UUID único de la zona de riego
  id          String            @id @default(uuid())
  /// Bancal al que pertenece esta zona
  plotId      String
  /// Farm propietaria de la zona
  farmId      String
  /// Nombre descriptivo de la zona
  name        String
  /// Descripción adicional de la zona
  description String?
  /// Área cubierta en metros cuadrados
  area        Float?
  /// Caudal de agua en litros por minuto
  flowRate    Float?
  /// Estado actual del sistema de riego
  status      IrrigationStatus  @default(INACTIVE)
  /// Si la zona está activa para programación
  isActive    Boolean           @default(true)
  /// Fecha de creación
  createdAt   DateTime          @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime          @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Farm propietaria
  farm            Farm              @relation(fields: [farmId], references: [id])
  /// Bancal donde está ubicada
  plot            Plot              @relation(fields: [plotId], references: [id])
  /// Válvulas de control de la zona
  valves          IrrigationValve[]
  /// Programaciones de riego
  schedules       IrrigationSchedule[]
  /// Logs de actividad de riego
  logs            IrrigationLog[]
  /// Reglas de automatización
  automationRules AutomationRule[]
  
  @@map("irrigation_zones")
}

/**
 * Válvula de riego
 * 
 * Actuador físico que controla el flujo de agua
 * en una zona específica. Controlable remotamente
 * mediante GPIO en nodos IoT.
 */
model IrrigationValve {
  /// UUID único de la válvula
  id        String      @id @default(uuid())
  /// Zona de riego a la que pertenece
  zoneId    String
  /// Nombre descriptivo de la válvula
  name      String
  /// Pin GPIO para control (en nodo IoT)
  pin       Int?
  /// Estado actual de la válvula
  status    ValveStatus @default(CLOSED)
  /// Última vez que se abrió
  lastOpened DateTime?
  /// Última vez que se cerró
  lastClosed DateTime?
  /// Fecha de instalación
  createdAt DateTime    @default(now())
  /// Fecha de última actualización
  updatedAt DateTime    @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  // Relations
  /// Zona de riego que controla
  zone IrrigationZone @relation(fields: [zoneId], references: [id])
  
  @@map("irrigation_valves")
}

/**
 * Programación de riego
 * 
 * Horario automático configurado para una zona específica.
 * Incluye días de la semana, hora de inicio, duración
 * y condiciones meteorológicas a verificar.
 */
model IrrigationSchedule {
  /// UUID único de la programación
  id          String   @id @default(uuid())
  /// Zona de riego programada
  zoneId      String
  /// Nombre descriptivo de la programación
  name        String
  /// Hora de inicio en formato HH:MM
  startTime   String
  /// Duración del riego en minutos
  duration    Int
  /// Días de la semana (0-6, domingo=0)
  daysOfWeek  Int[]
  /// Si la programación está activa
  isActive    Boolean  @default(true)
  /// Condiciones meteorológicas a verificar
  conditions  Json? // { maxRain: number, minTemperature: number, ... }
  /// Fecha de creación
  createdAt   DateTime @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Zona de riego programada
  zone IrrigationZone @relation(fields: [zoneId], references: [id])
  
  @@map("irrigation_schedules")
}

/**
 * Registro de actividad de riego
 * 
 * Log de cada sesión de riego ejecutada, incluyendo
 * duración real, consumo de agua y trigger que lo activó.
 */
model IrrigationLog {
  /// UUID único del registro
  id        String            @id @default(uuid())
  /// Zona donde se ejecutó el riego
  zoneId    String
  /// Usuario que inició el riego (si fue manual)
  userId    String?
  /// Momento de inicio del riego
  startTime DateTime
  /// Momento de finalización del riego
  endTime   DateTime?
  /// Duración real en minutos
  duration  Int?
  /// Estado final del riego
  status    IrrigationStatus
  /// Qué disparó el riego
  trigger   String // "manual", "scheduled", "sensor", "ai"
  /// Cantidad de agua utilizada en litros
  waterUsed Float?
  /// Observaciones adicionales
  notes     String?
  /// Fecha de creación del registro
  createdAt DateTime          @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  // Relations
  /// Zona donde se ejecutó
  zone IrrigationZone @relation(fields: [zoneId], references: [id])
  /// Usuario que ejecutó (si fue manual)
  user User?          @relation(fields: [userId], references: [id])
  
  @@map("irrigation_logs")
}

// ================================
// 🤖 AUTOMATION SYSTEM
// ================================
// Motor de reglas configurable para automatización completa:
// riego por sensores, horarios y control basado en IA

/**
 * Regla de automatización
 * 
 * Define lógica de control automático basada en condiciones
 * de sensores, horarios o IA. Cada regla puede disparar
 * múltiples acciones cuando se cumplen las condiciones.
 */
model AutomationRule {
  /// UUID único de la regla
  id          String         @id @default(uuid())
  /// Nombre descriptivo de la regla
  name        String
  /// Descripción del propósito de la regla
  description String?
  /// Zona de riego asociada (opcional)
  zoneId      String?
  /// Condiciones que disparan la regla
  conditions  Json // { sensorType: string, operator: string, value: number, ... }
  /// Acciones a ejecutar cuando se dispara
  actions     Json // { type: string, parameters: object, ... }
  /// Modo de operación de la automatización
  mode        AutomationMode @default(SENSOR_BASED)
  /// Si la regla está activa
  isActive    Boolean        @default(true)
  /// Prioridad de ejecución (mayor número = mayor prioridad)
  priority    Int            @default(0)
  /// Fecha de creación
  createdAt   DateTime       @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime       @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Zona de riego asociada
  zone            IrrigationZone?      @relation(fields: [zoneId], references: [id])
  /// Logs de ejecución de la regla
  executionLogs   AutomationExecution[]
  
  @@map("automation_rules")
}

/**
 * Ejecución de automatización
 * 
 * Registro de cada vez que se evalúa y ejecuta una regla
 * de automatización, incluyendo resultado y errores.
 */
model AutomationExecution {
  /// UUID único de la ejecución
  id           String   @id @default(uuid())
  /// Regla que se ejecutó
  ruleId       String
  /// Si la regla se disparó efectivamente
  triggered    Boolean
  /// Estado de las condiciones al momento de evaluación
  conditions   Json // { condition1: { evaluated: true, value: 25 }, ... }
  /// Acciones que se ejecutaron
  actions      Json // { action1: { executed: true, result: "success" }, ... }
  /// Si la ejecución fue exitosa
  success      Boolean
  /// Mensaje de error (si hubo fallo)
  errorMessage String?
  /// Momento de la ejecución
  executedAt   DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt    DateTime?
  
  // Relations
  /// Regla que se ejecutó
  rule AutomationRule @relation(fields: [ruleId], references: [id])
  
  @@map("automation_executions")
}

// ================================
// 🚨 ALERTS AND NOTIFICATIONS
// ================================
// Sistema de alertas en tiempo real y notificaciones
// multi-canal: email, SMS, Telegram, Slack, Discord

/**
 * Alerta del sistema
 * 
 * Notificación de eventos importantes que requieren atención:
 * fallos de sensores, condiciones críticas, mantenimiento, etc.
 * Incluye sistema de asignación y seguimiento de resolución.
 */
model Alert {
  /// UUID único de la alerta
  id          String     @id @default(uuid())
  /// Farm donde se generó la alerta
  farmId      String
  /// Nodo IoT que generó la alerta (opcional)
  nodeId      String?
  /// Tipo de alerta generada
  type        AlertType
  /// Nivel de severidad
  level       AlertLevel
  /// Título descriptivo de la alerta
  title       String
  /// Mensaje detallado de la alerta
  message     String
  /// Si la alerta ha sido leída
  isRead      Boolean    @default(false)
  /// Si la alerta ha sido resuelta
  isResolved  Boolean    @default(false)
  /// Usuario que creó la alerta
  createdById String?
  /// Usuario asignado para resolver la alerta
  assignedToId String?
  /// Momento en que se resolvió la alerta
  resolvedAt  DateTime?
  /// Fecha de creación de la alerta
  createdAt   DateTime   @default(now())
  /// Fecha de última actualización
  updatedAt   DateTime   @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Farm donde se generó
  farm       Farm     @relation(fields: [farmId], references: [id])
  /// Nodo IoT que la generó
  node       IoTNode? @relation(fields: [nodeId], references: [id])
  /// Usuario que creó la alerta
  createdBy  User?    @relation("AlertCreatedBy", fields: [createdById], references: [id])
  /// Usuario asignado para resolverla
  assignedTo User?    @relation("AlertAssignedTo", fields: [assignedToId], references: [id])
  
  @@map("alerts")
}

/**
 * Notificación
 * 
 * Mensaje enviado a un usuario a través de diferentes canales:
 * email, SMS, push, Telegram, etc. Incluye tracking de entrega
 * y lectura para asegurar que llegan las comunicaciones críticas.
 */
model Notification {
  /// UUID único de la notificación
  id        String              @id @default(uuid())
  /// Usuario destinatario
  userId    String
  /// Canal de comunicación utilizado
  channel   NotificationChannel
  /// Título de la notificación
  title     String
  /// Contenido del mensaje
  message   String
  /// Datos adicionales (JSON payload)
  data      Json? // { alertId: string, actionUrl: string, ... }
  /// Si la notificación fue enviada exitosamente
  sent      Boolean             @default(false)
  /// Momento en que se envió
  sentAt    DateTime?
  /// Si el usuario leyó la notificación
  read      Boolean             @default(false)
  /// Momento en que se leyó
  readAt    DateTime?
  /// Fecha de creación
  createdAt DateTime            @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  // Relations
  /// Usuario destinatario
  user User @relation(fields: [userId], references: [id])
  
  @@map("notifications")
}

// ================================
// 🌤️ WEATHER AND ENVIRONMENTAL DATA
// ================================
// Estación meteorológica local integrada con APIs externas
// (OpenWeather, AEMET) para decisiones agrícolas inteligentes

/**
 * Datos meteorológicos
 * 
 * Información climática local obtenida de estación meteorológica
 * propia o APIs externas (OpenWeather, AEMET). Utilizada para
 * decisiones automáticas de riego y alertas de condiciones adversas.
 */
model WeatherData {
  /// UUID único del registro meteorológico
  id           String           @id @default(uuid())
  /// Farm a la que corresponde el dato
  farmId       String
  /// Ubicación específica de la medición
  location     String
  /// Temperatura actual en grados Celsius
  temperature  Float
  /// Humedad relativa del aire (%)
  humidity     Float
  /// Presión atmosférica en hPa
  pressure     Float
  /// Velocidad del viento en km/h
  windSpeed    Float?
  /// Dirección del viento en grados (0-360)
  windDirection Float?
  /// Precipitación acumulada en mm
  rainfall     Float?
  /// Índice UV
  uv          Float?
  /// Condición meteorológica general
  condition    WeatherCondition
  /// Datos de pronóstico extendido
  forecast     Json? // { hourly: [], daily: [], alerts: [] }
  /// Fuente de los datos meteorológicos
  source       String // "local", "openweather", "aemet"
  /// Momento de la medición/obtención
  timestamp    DateTime         @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt    DateTime?
  
  // Relations
  /// Farm a la que pertenece
  farm Farm @relation(fields: [farmId], references: [id])
  
  /// Índice para consultas eficientes por farm y tiempo
  @@index([farmId, timestamp])
  /// Índice para consultas por ubicación y tiempo
  @@index([location, timestamp])
  @@map("weather_data")
}

// ================================
// 🔋 ENERGY MANAGEMENT
// ================================
// Gestión energética sostenible: paneles solares, baterías
// y optimización automática del consumo por IA

/**
 * Panel solar
 * 
 * Panel fotovoltaico instalado en la farm para generación
 * de energía renovable. Incluye especificaciones técnicas
 * y monitoreo de producción energética.
 */
model SolarPanel {
  /// UUID único del panel solar
  id           String    @id @default(uuid())
  /// Farm donde está instalado
  farmId       String
  /// Nodo IoT que monitorea el panel (opcional)
  nodeId       String?
  /// Nombre descriptivo del panel
  name         String
  /// Capacidad máxima en watts
  capacity     Float
  /// Eficiencia del panel (%)
  efficiency   Float?
  /// Ángulo de inclinación en grados
  tiltAngle    Float?
  /// Ángulo de azimut en grados (orientación)
  azimuthAngle Float?
  /// Fecha de instalación
  installDate  DateTime?
  /// Si el panel está activo
  isActive     Boolean   @default(true)
  /// Fecha de creación del registro
  createdAt    DateTime  @default(now())
  /// Fecha de última actualización
  updatedAt    DateTime  @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt    DateTime?
  
  // Relations
  /// Farm donde está instalado
  farm       Farm        @relation(fields: [farmId], references: [id])
  /// Logs de generación energética
  energyLogs EnergyLog[]
  
  @@map("solar_panels")
}

/**
 * Batería del sistema
 * 
 * Sistema de almacenamiento energético para autonomía
 * durante períodos sin sol. Monitorea salud, ciclos
 * de carga y capacidad residual.
 */
model Battery {
  /// UUID único de la batería
  id           String    @id @default(uuid())
  /// Farm donde está instalada
  farmId       String
  /// Nodo IoT que monitorea la batería (opcional)
  nodeId       String?
  /// Nombre descriptivo de la batería
  name         String
  /// Capacidad nominal en amperios-hora (Ah)
  capacity     Float
  /// Voltaje nominal en volts (V)
  voltage      Float
  /// Química de la batería
  chemistry    String? // "LiPo", "Li-ion", "Lead-acid", "LiFePO4"
  /// Número de ciclos de carga completados
  cycleCount   Int       @default(0)
  /// Estado de salud de la batería (%)
  healthStatus Float?    @default(100)
  /// Fecha de instalación
  installDate  DateTime?
  /// Si la batería está activa
  isActive     Boolean   @default(true)
  /// Fecha de creación del registro
  createdAt    DateTime  @default(now())
  /// Fecha de última actualización
  updatedAt    DateTime  @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt    DateTime?
  
  // Relations
  /// Farm donde está instalada
  farm       Farm        @relation(fields: [farmId], references: [id])
  /// Logs de actividad energética
  energyLogs EnergyLog[]
  
  @@map("batteries")
}

/**
 * Registro de energía
 * 
 * Log de producción, consumo y almacenamiento energético
 * del sistema. Permite análisis de eficiencia y autonomía
 * para optimización energética mediante IA.
 */
model EnergyLog {
  /// UUID único del registro energético
  id              String       @id @default(uuid())
  /// Panel solar que generó energía (opcional)
  solarPanelId    String?
  /// Batería involucrada en el registro (opcional)
  batteryId       String?
  /// Energía generada por paneles solares (W)
  generated       Float?
  /// Energía consumida por el sistema (W)
  consumed        Float?
  /// Energía almacenada en baterías (W)
  stored          Float?
  /// Nivel de carga de la batería (%)
  batteryLevel    Float?
  /// Voltaje actual de la batería (V)
  batteryVoltage  Float?
  /// Corriente de la batería (A, + carga, - descarga)
  batteryCurrent  Float?
  /// Consumo de la red eléctrica externa (W)
  gridConsumption Float?
  /// Eficiencia general del sistema (%)
  efficiency      Float?
  /// Momento del registro
  timestamp       DateTime     @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt       DateTime?
  
  // Relations
  /// Panel solar que generó la energía
  solarPanel SolarPanel? @relation(fields: [solarPanelId], references: [id])
  /// Batería involucrada
  battery    Battery?    @relation(fields: [batteryId], references: [id])
  
  /// Índice para consultas eficientes por tiempo
  @@index([timestamp])
  @@map("energy_logs")
}

// ================================
// 🧠 ANALYTICS AND AI
// ================================
// Motor de inteligencia artificial: predicciones de rendimiento,
// asistente conversacional y análisis predictivo avanzado

/**
 * Predicción de IA
 * 
 * Resultado de modelos de machine learning aplicados
 * a diferentes aspectos del sistema: rendimiento de cultivos,
 * necesidades de riego, condiciones meteorológicas, etc.
 */
model Prediction {
  /// UUID único de la predicción
  id          String   @id @default(uuid())
  /// Tipo de predicción realizada
  type        String // "yield", "irrigation", "weather", "pest", "harvest"
  /// Descripción de qué se está prediciendo
  target      String // "crop_yield", "irrigation_need", "rainfall", etc.
  /// ID de la entidad objetivo (plot, crop, etc.)
  targetId    String?
  /// Modelo de IA utilizado para la predicción
  model       String // "random_forest", "neural_network", "lstm", etc.
  /// Nivel de confianza de la predicción (0-1)
  confidence  Float
  /// Datos de la predicción
  prediction  Json // { value: number, range: {min, max}, factors: [] }
  /// Valor real para entrenamiento posterior
  actualValue Json?
  /// Fecha hasta que es válida la predicción
  validUntil  DateTime?
  /// Fecha de creación de la predicción
  createdAt   DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  /// Índice para consultas eficientes por tipo y objetivo
  @@index([type, targetId])
  @@map("predictions")
}

/**
 * Interacción con asistente IA
 * 
 * Registro de conversaciones con el chatbot/asistente
 * inteligente del sistema. Incluye feedback para mejora
 * continua del modelo de lenguaje.
 */
model AIInteraction {
  /// UUID único de la interacción
  id        String   @id @default(uuid())
  /// Usuario que interactuó con la IA
  userId    String
  /// Pregunta o consulta del usuario
  query     String
  /// Respuesta generada por la IA
  response  String
  /// Contexto de la conversación
  context   Json? // { conversation_id: string, previous_queries: [], farm_context: {} }
  /// Calificación del usuario (1-5 estrellas)
  feedback  Int?
  /// Fecha de la interacción
  createdAt DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  // Relations
  /// Usuario que realizó la consulta
  user User @relation(fields: [userId], references: [id])
  
  @@map("ai_interactions")
}

/**
 * Reporte de analíticas
 * 
 * Reporte automático generado por el sistema de analíticas
 * con métricas de rendimiento, eficiencia y tendencias
 * a lo largo de diferentes períodos temporales.
 */
model AnalyticsReport {
  /// UUID único del reporte
  id         String   @id @default(uuid())
  /// Nombre descriptivo del reporte
  name       String
  /// Tipo de reporte generado
  type       String // "yield", "efficiency", "consumption", "energy", "alerts"
  /// Período temporal del reporte
  period     String // "daily", "weekly", "monthly", "yearly", "seasonal"
  /// Datos del reporte en formato JSON
  data       Json // { metrics: {}, charts: [], summary: "", recommendations: [] }
  /// Fecha de generación del reporte
  generatedAt DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt  DateTime?
  
  @@map("analytics_reports")
}

// ================================
// 🔧 MAINTENANCE AND SYSTEM LOGS
// ================================
// Seguimiento de mantenimiento preventivo y logs del sistema
// para monitoreo completo y diagnóstico de problemas

/**
 * Registro de mantenimiento
 * 
 * Log de actividades de mantenimiento realizadas en el sistema:
 * limpieza, calibración, reparaciones, reemplazos. Incluye
 * programación de mantenimiento preventivo.
 */
model MaintenanceLog {
  /// UUID único del registro de mantenimiento
  id          String   @id @default(uuid())
  /// Usuario que realizó el mantenimiento
  userId      String
  /// Nodo IoT mantenido (opcional)
  nodeId      String?
  /// Tipo de mantenimiento realizado
  type        String // "cleaning", "calibration", "repair", "replacement", "inspection"
  /// Descripción detallada del trabajo realizado
  description String
  /// Costo del mantenimiento (opcional)
  cost        Float?
  /// Fecha programada del próximo mantenimiento
  nextDue     DateTime?
  /// Fecha en que se completó el mantenimiento
  completedAt DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt   DateTime?
  
  // Relations
  /// Usuario que realizó el mantenimiento
  user User @relation(fields: [userId], references: [id])
  
  @@map("maintenance_logs")
}

/**
 * Log del sistema
 * 
 * Registro de eventos técnicos del sistema para debugging,
 * monitoreo y auditoría. Incluye diferentes niveles de
 * severidad y componentes del sistema.
 */
model SystemLog {
  /// UUID único del log
  id        String   @id @default(uuid())
  /// Nivel de severidad del log
  level     String // "info", "warning", "error", "debug", "critical"
  /// Componente del sistema que generó el log
  component String // "backend", "node", "sensor", "irrigation", "energy", "ai"
  /// Mensaje descriptivo del evento
  message   String
  /// Datos adicionales del evento
  data      Json? // { error_code: string, stack_trace: string, context: {} }
  /// Momento en que ocurrió el evento
  timestamp DateTime @default(now())
  /// Soft delete: registro eliminado lógicamente
  deletedAt DateTime?
  
  /// Índice para consultas eficientes por nivel y tiempo
  @@index([level, timestamp])
  /// Índice para consultas eficientes por componente y tiempo
  @@index([component, timestamp])
  @@map("system_logs")
}

// ================================
// ⚙️ CONFIGURATIONS
// ================================
// Configuraciones del sistema por farm: umbrales de sensores,
// preferencias de alertas y parámetros de automatización

/**
 * Configuración del sistema (por farm)
 * 
 * Parámetros configurables por farm para personalizar
 * el comportamiento del sistema: umbrales de sensores,
 * preferencias de alertas, configuración de IA, etc.
 */
model FarmConfiguration {
  /// UUID único de la configuración
  id        String   @id @default(uuid())
  /// Farm a la que pertenece la configuración
  farmId    String
  /// Notificaciones por email
  emailNotifications        Boolean  @default(true)
  /// Notificaciones por SMS
  smsNotifications         Boolean  @default(false)
  /// Notificaciones por push
  pushNotifications        Boolean  @default(true)
  /// Umbral de alerta crítica
  criticalAlertThreshold   Int?     // Por ejemplo, nivel de batería mínimo
  /// Modo de ahorro de energía nocturno
  energySavingMode         Boolean  @default(false)
  /// Fuente de energía preferida
  preferredEnergySource    EnergySource @default(SOLAR)
  /// Habilitado Home Assistant
  homeAssistantEnabled      Boolean  @default(false)
  /// Habilitado Telegram
  telegramEnabled           Boolean  @default(false)
  /// Habilitado Slack
  slackEnabled              Boolean  @default(false)
  // /// Idioma por defecto
  // defaultLanguage          String   @default("es")
  /// Zona horaria
  timezone               String   @default("UTC")
  /// Si la configuración está activa
  isActive                  Boolean  @default(true)
  /// Fecha de creación
  createdAt                 DateTime @default(now())
  /// Fecha de última actualización
  updatedAt                 DateTime @updatedAt
  /// Soft delete: registro eliminado lógicamente
  deletedAt                 DateTime?
  
  // Relations
  /// Farm a la que pertenece
  farm Farm @relation(fields: [farmId], references: [id])
  
  /// Una configuración por farm
  @@unique([farmId])
  @@map("farm_configuration")
}

/**
 * ===================================================================
 * 📊 SCHEMA SUMMARY
 * ===================================================================
 * 
 * MODELS TOTAL: 30+
 * ENUMS: 12 categorías con 90+ valores
 * 
 * KEY FEATURES:
 * ✅ Multi-tenant architecture (Farm-based)
 * ✅ IoT ready with sensor networks
 * ✅ Automated irrigation with smart rules
 * ✅ Solar energy management
 * ✅ AI predictions and analytics
 * ✅ Real-time alerts and notifications
 * ✅ Comprehensive audit trails
 * ✅ Optimized indexes for performance
 * 
 * RELATIONSHIPS:
 * - User → FarmMembership → Farm (many-to-many with roles)
 * - Farm → Plots → Crops (hierarchical structure)
 * - IoTNode → Sensors → SensorReadings (telemetry data)
 * - IrrigationZone → AutomationRules (smart control)
 * - Farm → SolarPanel/Battery → EnergyLog (sustainability)
 * 
 * ===================================================================
 */